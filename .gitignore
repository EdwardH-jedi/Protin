# --- Python ---
__pycache__/
*.py[cod]
*.pyo
.venv/
.env
.env.staging
.env.production

# --- Node ---
node_modules/
*.tsbuildinfo

# --- SSL certificates (generated locally — never commit) ---
infra/nginx/certs/*.crt
infra/nginx/certs/*.key
infra/nginx/certs/*.pem

# --- Expo / React Native ---
.expo/
.expo-shared/
dist/
web-build/

# --- Misc ---
resume/
.DS_Store

# Claude Code — per-developer local state (do not commit)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Codex Stop-hook review reports (generated)
reviews/

# Claude Code — run archive (per-developer local; README only is tracked)
runs/*
!runs/README.md

# Claude Code — hook state (session → run-file mapping)
.claude/tmp/

# Local development artifacts
apps/api/.venv_win/
apps/api/media/
references/
