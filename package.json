{
  "name": "sportsgang",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:reset": "docker compose down -v && docker compose up -d",
    "infra:logs": "docker compose logs -f",
    "infra:ps": "docker compose ps",
    "mobile:start": "npm run start --workspace @sportsgang/mobile",
    "mobile:android": "npm run android --workspace @sportsgang/mobile",
    "mobile:ios": "npm run ios --workspace @sportsgang/mobile",
    "mobile:web": "npm run web --workspace @sportsgang/mobile"
  },
  "devDependencies": {
    "@expo/config-plugins": "~54.0.4"
  }
}
